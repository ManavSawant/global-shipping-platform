spring:
  application:
    name: global-shipping-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/shipping_db
    username: mns666
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true